import React from "react";
import { View, Text, Pressable, StyleSheet } from "react-native";
import { MenuItem } from "../models/types";

export function MenuItemCard({
  item,
  onAdd,
}: {
  item: MenuItem;
  onAdd: () => void;
}) {
  return (
    <View style={styles.card}>
      <View style={{ flex: 1 }}>
        <Text style={styles.title}>{item.name}</Text>
        <Text style={styles.desc}>{item.description}</Text>
        <Text style={styles.price}>{item.price.toLocaleString()} RWF</Text>
      </View>

      <Pressable onPress={onAdd} style={styles.btn}>
        <Text style={styles.btnText}>Add</Text>
      </Pressable>
    </View>
  );
}

const styles = StyleSheet.create({
  card: {
    padding: 14,
    borderRadius: 14,
    backgroundColor: "#111827",
    marginBottom: 10,
    flexDirection: "row",
    gap: 12,
    alignItems: "center",
  },
  title: { color: "white", fontSize: 16, fontWeight: "700" },
  desc: { color: "#cbd5e1", marginTop: 6, fontSize: 13 },
  price: { color: "#f59e0b", marginTop: 8, fontWeight: "700" },
  btn: {
    backgroundColor: "#f59e0b",
    paddingHorizontal: 14,
    paddingVertical: 10,
    borderRadius: 12,
  },
  btnText: { fontWeight: "800", color: "#111827" },
});
